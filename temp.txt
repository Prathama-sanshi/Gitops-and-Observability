# Step 1:
helm install argocd . -f values.yaml -n argocd

# Step 2: get password for argocd 
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d


# Step: 3 - Port forward 
 kubectl port-forward service/argocd-server -n argocd 8080:443
#http://localhost:8080

#Step :4 - Create a project
cd argocd/resources/
kubectl apply -f app-project.yaml

#step5: - create app of apps 
kubectl apply -f argocd-application.yaml

#step:6 deploy products:
cd Application/apps/overlays/dev-cluster/kustomization.yaml


#Step: 7 Get Prometheus URL
kubectl port-forward svc/kube-prometheus-stack-prometheus 9090:9090 -n observability
http://localhost:9090

#Step : 8 Get Grafana URL
secret:
kubectl get secret --namespace observability -l app.kubernetes.io/component=admin-secret -o jsonpath="{.items[0].data.admin-password}" | base64 --decode ; echo
kubectl --namespace observability port-forward $POD_NAME 3000
http://localhost:3000
(upload dashboard json file)




cat ~/.ssh/id_ed25519_argocd.pub
cat ~/.ssh/id_ed25519_argocd
Application/apps/base/file-monitor/versions/app-1.0.0




ARGOCD LOGIN:
argocd login localhost:8080 --insecure
